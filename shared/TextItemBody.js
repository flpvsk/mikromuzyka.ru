import styled from '@emotion/styled';
import TextBody from '../shared/TextBody';

const TextItemBody = styled(TextBody)();
TextItemBody.defaultProps = {
  fontSize: [1, 1, 2, 2],
  mb: 3,
};

export default TextItemBody;
